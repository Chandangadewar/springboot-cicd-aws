global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'springboot-api'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['app:8080']
    relabel_configs:
      - target_label: application
        replacement: springboot-api
